name: Run JMeter ReqRes Tests

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '15 8 * * *'  # 15:15 WIB (UTC+7)

jobs:
  jmeter-test:
    runs-on: ubuntu-latest

    steps:
    - name: 🔽 Checkout Repository
      uses: actions/checkout@v3

    - name: ☕️ Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: 🔧 Install JMeter
      run: |
        wget https://downloads.apache.org//jmeter/binaries/apache-jmeter-5.6.3.tgz
        tar -xzf apache-jmeter-5.6.3.tgz
        echo "JMETER_HOME=$(pwd)/apache-jmeter-5.6.3" >> $GITHUB_ENV

    - name: ▶️ Run JMeter Test Plan
      run: |
        mkdir -p logs/html-report
        $JMETER_HOME/bin/jmeter -n \
          -t tests/cdc-reqres.jmx \
          -l logs/result.jtl \
          -q configs/secret.properties
        $JMETER_HOME/bin/jmeter -g logs/result.jtl -o logs/html-report

    - name: 📤 Upload JMeter Results & Report
      uses: actions/upload-artifact@v4
      with:
        name: jmeter-report
        path: |
          logs/result.jtl
          logs/html-report/
        compression-level: 6
        if-no-files-found: warn

    - name: 📢 Telegram Notification (optional)
      if: always()
      run: |
        STATUS="✅ JMeter test passed"
        [ "${{ job.status }}" != "success" ] && STATUS="❌ JMeter test failed"
        
        TIMESTAMP=$(date -u '+%Y-%m-%d %H:%M UTC')
        MESSAGE=$(cat <<EOF
        $STATUS

        🕒 Completed at: $TIMESTAMP
        📊 JMeter `.jtl` and HTML report available in artifacts
        EOF
        )
        curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d parse_mode="Markdown" \
          --data-urlencode "text=$MESSAGE"